@helper TableHeader(params string[] headers)
{
    <thead>
        <tr>
        @foreach(var header in headers)
        {
            <th class="hidden-480">
                @header
            </th>
        }
        </tr>
    </thead>
}